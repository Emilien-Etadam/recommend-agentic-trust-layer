# Verification report — Run an Attested Computation on BigQuery

*recommend-trust-layer/0.1 · 2026-07-29T18:25:15Z · mode: citation-integrity (bundle sources as evidence)*

**Outcome: INCONCLUSIVE**

- **no-evidence** · `[^(uncited)]` — Caller has a service account with bigquery.jobs.create on the concept's resource project.
  - uncited claim and no readable source to check it against
- **no-evidence** · `[^(uncited)]` — Caller has read access on every table referenced by the computation's # Computation fence (or the file at computation:).
  - uncited claim and no readable source to check it against
- **no-evidence** · `[^(uncited)]` — The concept declares its parameters: list. Every required parameter has been supplied a value by the caller.
  - uncited claim and no readable source to check it against
